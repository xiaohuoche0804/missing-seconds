# 小说创作标准流程

## 核心原则
**写完 ≠ 完成。质检通过 + Git同步 = 完成。**

---

## 创作流程（每章节必须走完）

```
步骤1：写章节
    ↓
步骤2：质检（调用 reviewer）
    ↓
步骤3：等待质检报告
    ↓
步骤4：若判决=pass → 步骤5
    若判决=minor_fix → 修复 → 步骤3（复检）
    若判决=rewrite → 重写 → 步骤3（复检）
    ↓
步骤5：质检通过后 git add + commit + push
    ↓
步骤6：发布到番茄（publisher）
```

**关键约束**：
- 任何章节未通过质检，不得 git push
- 修复后必须复检通过，才能进行下一步
- 发布前必须先运行 `publish_clean.py` 清理元数据

---

## 当前进度追踪

| 范围 | 状态 |
|------|------|
| 第1-31章 | ✅ 全部质检通过 |
| 第32-53章 | 🔄 复检中（修复5个问题后待复检） |
| 第54-60章 | 🔄 复检中（修复4个问题后待复检） |
| 第6章发布 | 🔄 publisher进行中 |

---

## 质检报告目录
每次质检报告必须保存到：
`小说/缺失的七秒/质检报告/`

文件名格式：`第XX-YY章质检报告.md`

---

## 章节清理规则（发布前必执行）
```bash
cd 小说/缺失的七秒/
python3 publish_clean.py all   # 发布前先清理所有章节
```

清理内容：章节标签、本章完、本章摘要、本章字数、标题前的 `#`

---

## Git同步时机
**正确**：质检 pass → git sync
**错误**：写完就 push，不等质检

---

## 当前待完成任务
1. 等待 32-53章复检结果
2. 等待 54-60章复检结果
3. 第6章发布完成后，继续发布第7-N章（每章必须先质检）
4. 所有章节质检通过后，统一发布番茄
